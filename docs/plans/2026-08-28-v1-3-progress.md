# SuperTask 1.3 进度快照

> 日期：2026-08-28  
> 状态：**phase 1（模型与兼容层）完成；未 commit**  
> 仓库：`<repository-root>`（本地 `main`，无 remote）  
> 功能规格：[2026-08-28-v1-3-feature-spec.md](2026-08-28-v1-3-feature-spec.md)  
> 实现计划：[2026-08-28-v1-3-implementation-plan.md](2026-08-28-v1-3-implementation-plan.md)

## 一句话

**已完成：1.3 phase 1（docker typed 段 + `kind: compose` 校验 + 11 个错误码）。**  
`cargo test -p supertask-core` → **162 passed, 1 ignored**；`cargo check -p supertask` 通过；前端 `npm run build` 通过。`docker` feature 仍 soon；`kind: compose` 可解析不可启动（launcher 仍 `KIND_UNSUPPORTED`，phase 3 接入）。

## Phase 1 已落地

- `error.rs` — §10.1 十一个稳定码（`DOCKER_NOT_FOUND` … `IMAGE_BUILD_FAILED`），serde 序列化逐个断言。
- `spec/file.rs` — `SuperTaskFile.docker` 从 `Option<Value>` 转 typed `DockerSpec`（`compose_file`/`project_name`/`builds` + flatten extra）；`DockerBuild`（name/context/dockerfile/tags + extra）；`ServiceSpec.service: Option<String>`。
- `spec/validate.rs` — `validate_v13`：compose_file/context/dockerfile 相对路径（禁绝对、盘符、`..` 段）、`project_name`/`service` 字符集 `^[A-Za-z0-9][A-Za-z0-9_.-]{0,63}$`、builds name 唯一、tags 至少一条且禁 `--` 前缀。`kind: compose` 独立校验分支：`service` 必填；`env`/`env_file`/`extra_args`/`build_args`/`jvm_args`/`cwd`/`restart`/`module`/`dir`/`package_manager`/`launch` 出现即 `SPEC_INVALID`（防用户以为会向容器注入环境）；不再落入 `KIND_UNSUPPORTED` 警告分支。
- `scan.rs` / `launcher.rs` / `tests/spec_yaml.rs` — 跟进新字段与 compose 语义（launcher 测试改为断言 compose 无警告 + 启动仍 `KIND_UNSUPPORTED`）。
- `frontend/src/ipc/protocol.ts` — `DockerSpec`/`DockerBuild`/`ServiceSpec.service` 类型同步。
- 文档：`docs/spec/yaml.md`（顶层 `network`/`log_retention`/`toolchain` 行、§4.1 `service` 行、§4.2 compose kind 行）、`docs/spec/ipc.md`（§4.6 toolchain install/upgrade live、§4.8 占位命令去掉 toolchain）；新建本计划与本文档。

**新测试：** 错误码序列化 ×11、compose/docker 段 round-trip（含未知键）、`service` 缺失/字符集、五个注入字段拒绝、路径与 tag 规则、compose name 校验器。

## 1.2 遗漏核对结论（本轮顺带修掉）

真正遗漏（已修）：
1. `probe.rs` 两处"1.2 将支持一键安装"过时文案 → 改为指向「环境」页。
2. `yaml.md` / `ipc.md` 文档债（toolchain 标 reserved、install 标 SOON）→ 已按 1.2 phase 1+2 增补。

记录在案的 1.2 剩余（非遗漏，是 §18 步骤 3–7）：ports/secrets、日志搜索、指标、profile、jar、运行时接 `env_delta`/代理环境、mirror/registry 注入、真机验收。**1.3 对 1.2 的依赖**（§14：端口检查、crash 通知）后移，其余先行。

## 下次从哪接着

1. `cargo test -p supertask-core` 确认仍绿（162 passed, 1 ignored）。
2. **先 commit**：工作区已累积 1.2 phase 1/2 + 1.3 phase 1 三批变更（相对 `920885b`）。
3. 按计划做 **1.3 phase 2（Docker CLI 适配层）**：`crates/supertask-core/src/docker/`（runner/probe/compose_config + 缓存），fake fixture 测试，不真调 docker。
4. 1.2 phase 3–7 与 1.3 可并行推进，注意端口检查/crash 通知两处依赖。

## Git

- 分支：`main`；remote：无；**没有 commit**（自 `920885b` 起全部在工作区）。
