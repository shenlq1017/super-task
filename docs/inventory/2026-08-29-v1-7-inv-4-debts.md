# inv-4 · 遗留欠账清单

> 2026-08-29。盘点稿，供复核。只收各 progress 文档 / repository conventions **明确记名**的未完项；推测项不收。
> 复核方式：每条给出处文档与原文关键词，可 grep 复查。
> **2026-08-29 排期更新：A1–A4 已拍板进 v1.7**（[规格](../plans/2026-08-29-v1-7-feature-spec.md) §2.2 拍板 6；实现计划 Phase 4/5/8）；B/C 类单开验收专项，不阻塞开发。
> **2026-08-30：D1 部分消化** —— v2.0.1 云管理控制台交付了运营入口（`/admin/api/*` + `cloud-console/`），生产 HTTPS 与端点运营仍是欠账；见 [v2.0.1 规格](../plans/2026-08-30-v2-0-1-cloud-admin-console-spec.md)。

## A. 功能缺口（与横向扩展抢同一批工时）

| # | 欠账 | 出处 | 现状细节 |
|---|------|------|----------|
| A1 | **Maven mirror / npm registry 与 `env_delta` 完整运行时接线** | v1-2-progress `:21,50`；v1-3-progress `:55` | **2026-08-29 复核修正（二次）**：实际比 progress 记载更浅——`EffectiveNetwork` 只有代理字段（`network.rs:20-25`），maven/npm 镜像从未进入它（仅 YAML 层 `spec/file.rs:320-322`）；`tool_env` 全仓唯一调用方是壳层工具链安装链（`src-tauri/src/commands.rs:530`）；**前端网络 UI 也完全不存在**（原记「/env UI 已有」不实）。**已由 v1.7 关闭**：运行时注入 + /env 网络卡 + pip/goproxy 扩展 + env_delta 接线（显式 mise） |
| A2 | **系统级崩溃通知** | v1-2-progress `:14`；roadmap「崩溃通知｜状态灯不够｜1.2」 | 未做；`ServiceRuntime.last_exit` 已带退出码（ipc.md §6），前端改动即可。v1.7 Phase 5 |
| A3 | **服务分组等交互细化** | repository conventions 1.2 段；v1-3-progress `:55` | spec 字段已留（`spec/file.rs:79`），运行页无分组 UI（inv-3 §5）。v1.7 Phase 5 |
| A4 | Playwright 用例 | v1-6-progress `:4`；1.1 段 repository conventions | 未建。v1.7 Phase 8 起步（主链路冒烟） |

## B. 验收类（真机/人工）

| # | 欠账 | 出处 |
|---|------|------|
| B1 | Windows 真机验收（1.2 范畴） | v1-2-progress `:14` |
| B2 | Docker Desktop 真机验收（1.3 phase 7） | v1-3-progress `:12` |
| B3 | 1.4 打包与真机矩阵 | repository conventions 当前阶段；v1-4-progress |
| B4 | 1.5 §13.4 人工真机验收项 + macOS/Linux 实机抽验 | v1-5-progress `:79` |
| B5 | 1.6 §14.4 真机人工验收矩阵 | v1-6-progress `:4,59` |
| B6 | 1.1 真机剩余：真实 IDE/Git 环境 | repository conventions 1.1 段 |

## C. 发布工程

| # | 欠账 | 出处 |
|---|------|------|
| C1 | 签名密钥与 updater endpoint/pubkey 占位替换 | repository conventions 1.1 段 |
| C2 | 安装包 | repository conventions 1.1 段；roadmap 1.1「自动更新：否则 1.1 开始无法交付」 |

## D. 路线图上「未到点、按版本拒绝」的事项（不是欠账，是排期）

roadmap 版本地图 + repository conventions「已拍板 12」（没有到点就拒绝实现）：

| 事项 | 排期 | 备注 |
|------|------|------|
| Python / Go 服务 kind | **2.2**（yaml.md §4.2 同标注） | 本轮横向扩展的直接对象，提前需显式重排 |
| 插件 / 自定义 kind | 2.2 | 「避免核心膨胀」 |
| WSL2 后端 | 2.2 | Windows 上跑 Linux 工具链 |
| 云/账号/同步、一键迁移 | 2.0 | 导航已占位；1.5 导出包是「一键迁移的离线版」（roadmap 需求 9） |
| AI、README 生成 YAML | 2.1 | 占位中 |
| macOS/Linux | 1.4 已做主体 | 实机抽验在 B4 |
| Redis/MySQL sidecar | 1.3 已做（compose） | — |
| devcontainer / mise.toml 导出 | 2.2 备注项 | 「需要可复现时」 |

## F. v2.0 云新增欠账（本地自动化已完成，生产/兼容性验收分开记账）

| # | 欠账 | 当前事实 | 对应文档 |
|---|---|---|---|
| D1 | 自托管参考服务生产收口 | **部分消化（2026-08-30）**：运营入口已交付（`/admin/api/*` 十条 + `cloud-console/` 自带 Web 控制台，14 项 in-process 测试全绿并进 CI）；剩余欠账是正式 HTTPS 部署、TLS 证书与安全运营配置 | [cloud-server.md](../spec/cloud-server.md) §8、[v2.0.1 规格](../plans/2026-08-30-v2-0-1-cloud-admin-console-spec.md) |
| D2 | 未知 type 前向兼容 | 客户端逐项 skip 与参考 server 格式合法未知 type 保存已接线；剩余仅跨版本真机验收 | [cloud.md](../spec/cloud.md) |
| D3 | v2.0 真机与正式端点 | 官方运营方/端点未拍板；本地参考服务登录/同步和跨设备路径尚未真机验收；桌面端 `#/cloud`「被控制台停用后报认证失败」也仍属人工矩阵（协议层已 curl 证毕） | v2.0 实现计划 Phase 0/10 |
| D4 | 客户端云功能尾项 | `secrets.sync:true`/vault 编排、passphrase 管理 UI、模板拉取落盘仍待补 | v2.0 实现计划偏差备案 |
| D5 | 云会话吊销 | 管理面改密与控制台「退出登录」都**不吊销已签发 token**：access 15 分钟自然过期，refresh 30 天内仍可换新；设备列表与单 token 撤销、`POST /auth/logout` 均未做 | [cloud-server.md](../spec/cloud-server.md) §5/§8 |

## E. 汇总判断

- 1.x 功能与原 A 类缺口按既有条目维护；v2.0 客户端自动化范围已落地，但不等于服务端或真实端点已验收。
- 验收类（B1–B6）、发布工程（C1–C2）与上述 D 类是「横切债」，不随客户端 fake 测试通过自动消失。
