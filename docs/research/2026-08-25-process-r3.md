# 梳理过程（2026-08-25，第三轮）

用户确认技术选型，并要求深化 1.0 功能计划。

## 拍板

- 栈锁定：Tauri 2 + Rust + React + TypeScript（不再写「待点头」）  
- 1.0 规格从「功能列表」收到：状态机、yaml 校验、扫描算法、Windows 启动命令、IPC、错误码、十条验收  

## 深化时做的选择

- 无 yaml 时 **1.0 就扫描生成草稿**（否则多模块手写起不来）。路线里 1.1 的「生成 YAML」改理解为模板/向导升级，不是第一次生成。  
- Windows 停止 **直接 TerminateJobObject**，不做假优雅退出。  
- 表单保存丢注释，原文保存保全文；未知字段走 extra。  
- Node 默认依赖全部 spring 服务：粗，可改，1.0 够用。  
- `/env` 标 live：探测真做，安装仍 soon。
