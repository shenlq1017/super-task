# 梳理过程（2026-08-25，后端开工）

用户要求推进后台：占位可扩展、架构、性能与安全、YAML 规范、IPC（日志流单独建模）。

落地：

- `docs/spec/yaml.md` + JSON Schema：1.0 字段与 reserved 段（gateway/cloud/ai/…）具名保留  
- `docs/spec/ipc.md`：Command vs Event；日志 subscribe + 批次 `st.logs`，禁止按行 invoke  
- `crates/supertask-core`：解析/校验/拓扑/状态机/日志环/沙箱/启动 argv/Job Object  
- 未建 Tauri（UI 方案未定）。引擎不依赖 Tauri。

验证：`cargo test -p supertask-core`（含 ping 进 Job 后 Terminate 退出）。
