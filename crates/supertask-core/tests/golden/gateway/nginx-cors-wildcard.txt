# Generated by SuperTask — source of truth is supertask.yaml (gateway section).
# This file is regenerated on gateway start/apply; do not edit.
worker_processes 1;
daemon off;
pid "C:/ws/.supertask/gateway/nginx.pid";
error_log "C:/ws/.supertask/gateway/nginx-error.log";

events {
    worker_connections 1024;
}

http {
    access_log "C:/ws/.supertask/gateway/nginx-access.log";
    sendfile on;
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    server {
        listen 127.0.0.1:8080 default_server;
        server_name _;

        location /api {
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization" always;
            add_header Access-Control-Max-Age "600" always;
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin "*" always;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
                add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization" always;
                add_header Access-Control-Max-Age "600" always;
                return 204;
            }
            proxy_pass http://127.0.0.1:8081;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
        }
    }
}
