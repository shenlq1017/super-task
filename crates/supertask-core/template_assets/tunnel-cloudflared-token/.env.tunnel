# Cloudflare 命名隧道令牌（TUNNEL_TOKEN）
# 获取：Cloudflare Zero Trust → Networks → Tunnels → 创建/选择隧道 → 复制 token。
# 粘贴到下一行等号后，保存，然后启动 tunnel 服务。
# 该文件经 SuperTask env_file 注入进程环境，不会进入 supertask.yaml、日志或事件。
TUNNEL_TOKEN=
